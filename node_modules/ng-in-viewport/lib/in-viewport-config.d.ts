export declare enum InViewportConfigDirection {
    BOTH = "both",
    VERTICAL = "vertical",
    HORIZONTAL = "horizontal",
}
export interface InViewportConfigCheckFnOptions {
    force: boolean;
    config: InViewportConfig;
}
export declare type InViewportConfigCheckFn = (entry: IntersectionObserverEntry, options: InViewportConfigCheckFnOptions) => any;
export interface InViewportConfigOptions {
    root?: Element;
    rootMargin?: string;
    threshold?: number | number[];
    partial?: boolean;
    direction?: InViewportConfigDirection;
    checkFn?: InViewportConfigCheckFn;
}
export declare class InViewportConfig {
    private _root;
    private _rootMargin;
    private _threshold;
    private _partial;
    private _direction;
    private _checkFn;
    constructor(options?: InViewportConfigOptions);
    root: Element;
    rootMargin: string;
    threshold: number | number[];
    partial: boolean;
    direction: InViewportConfigDirection;
    checkFn: InViewportConfigCheckFn;
}
