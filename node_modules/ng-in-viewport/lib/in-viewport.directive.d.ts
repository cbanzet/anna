import { AfterViewInit, ElementRef, EventEmitter, OnDestroy } from '@angular/core';
import { InViewportService } from './in-viewport.service';
export declare const InViewportMetadata: unique symbol;
export declare class InViewportDirective implements AfterViewInit, OnDestroy {
    private platformId;
    private elementRef;
    private inViewport;
    private config;
    private readonly subscription;
    private options;
    readonly inViewportAction: EventEmitter<any>;
    constructor(platformId: Object, elementRef: ElementRef, inViewport: InViewportService);
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    private check(entry, force);
    private emitAction(entry, force);
}
